const Tip = ({ title }) => {
  return (
    <div className="bg-secondary-color w-32 text-center rounded py-1">
      <p className="font-primary-color font-space-grotesk">{title}</p>
    </div>
  );
};

export default Tip;
