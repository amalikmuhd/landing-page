// eslint-disable-next-line react/prop-types
const Text = ({ title }) => {
  return (
    <div className="my-4">
      <p className="text-6xl font-primary-color font-space-grotesk">{title}</p>
    </div>
  );
};

export default Text;
